# Optional. Without either, question parsing is rule-based only (fully
# functional). With a key, the LLM parses the question into the same
# validated QuerySpec - it never gets to run arbitrary code either way.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
