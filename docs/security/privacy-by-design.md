# Privacy by design — LLM Business Insight Assistant

- Uploaded CSVs are processed in memory for the session only — not
  persisted to any database or file in this MVP.
- No user accounts, no upload history.
- The public demo never uses a real uploaded file — only the bundled
  synthetic sample dataset.
